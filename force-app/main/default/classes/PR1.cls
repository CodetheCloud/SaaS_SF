public class PR1 {

     public pagereference pageRef(){
        pagereference Pr = new pagereference('https://google.com');
         //contact con= [select id, name from contact];
         //pagereference Pr = new pagereference('/'+'PR1?ID='con.ID);
        return pr;
     }
    
    
}